{% extends 'base.html.twig' %}

{% block title %}Modification du paramètre {{ setting.id }}{% endblock %}

{% block body %}
    <h1>Modification du paramètre {{ setting.id }}</h1>

    {{ include('setting/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('app_setting_index') }}" class="btn btn-secondary">Retour à la liste</a>

    {{ include('setting/_delete_form.html.twig') }}
{% endblock %}

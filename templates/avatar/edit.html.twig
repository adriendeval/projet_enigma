{% extends 'base.html.twig' %}

{% block title %}Modification de l'avatar {{ avatar.filename }}{% endblock %}

{% block body %}
    <h1>Modification de l'avatar {{ avatar.filename }}</h1>

    {{ include('avatar/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('app_avatar_index') }}" class="btn btn-secondary">Retour à la liste</a>

    {{ include('avatar/_delete_form.html.twig') }}
{% endblock %}

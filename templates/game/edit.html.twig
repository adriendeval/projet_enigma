{% extends 'base.html.twig' %}

{% block title %}Modification du jeu {{ game.title }}{% endblock %}

{% block body %}
    <h1>Modification du jeu {{ game.title }}</h1>

    {{ include('game/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('app_game_index') }}" class="btn btn-secondary">Retour à la liste</a>

    {{ include('game/_delete_form.html.twig') }}
{% endblock %}

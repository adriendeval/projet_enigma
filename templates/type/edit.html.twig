{% extends 'base.html.twig' %}

{% block title %}{{ type.label }} - Modification{% endblock %}

{% block body %}
    <h1>Modifier le type {{ type.label }}</h1>

    {{ include('type/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a href="{{ path('app_type_index') }}" class="btn btn-secondary">Retour</a>

    {{ include('type/_delete_form.html.twig') }}
{% endblock %}

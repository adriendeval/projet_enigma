{% extends 'base.html.twig' %}

{% block title %}Enigma{% endblock %}

{% block body %}
    <h1>Enigma</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enigma.id }}</td>
            </tr>
            <tr>
                <th>Position</th>
                <td>{{ enigma.position }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ enigma.title }}</td>
            </tr>
            <tr>
                <th>Instruction</th>
                <td>{{ enigma.instruction }}</td>
            </tr>
            <tr>
                <th>SecretCode</th>
                <td>{{ enigma.secretCode }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_enigma_index') }}">back to list</a>

    <a href="{{ path('app_enigma_edit', {'id': enigma.id}) }}">edit</a>

    {{ include('enigma/_delete_form.html.twig') }}
{% endblock %}

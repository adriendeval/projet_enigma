<form method="post" action="{{ path('app_enigma_delete', {'id': enigma.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer l\'énigme n°{{ enigma.id }} ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enigma.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}Enigma index{% endblock %}

{% block body %}
    <h1>Enigma index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for enigma in enigmas %}
            <tr>
                <td>{{ enigma.id }}</td>
                <td>{{ enigma.title }}</td>
                <td>{{ enigma.description }}</td>
                <td>
                    <a href="{{ path('app_enigma_show', {'id': enigma.id}) }}">show</a>
                    <a href="{{ path('app_enigma_edit', {'id': enigma.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_enigma_new') }}">Create new</a>
{% endblock %}
